header.header
  .container
    .row.align-items-center
      .col-3
        .inner-logo
          a(href="/")
            h2 Logo

      .col-9
        .inner-menu
          ul
            li
              a(href="/" class="text-dark") Trang chủ
            li
              a(href="/categories" class="text-dark") Danh mục
            li
              a(href="/cart" class="text-dark") Giỏ hàng (<span mini-cart>0</span>)
            li 
              a(href="/order" class="text-dark") Đơn hàng
                if(user)
                  span  (#{totalOrders})
            if(user)
              li 
                a(href="/users/logout" class="text-dark") Đăng xuất
            else
              li 
                a(href="/users/login" class="text-dark") Đăng nhập
              li 
                  a(href="/users/register" class="text-dark") Đăng ký